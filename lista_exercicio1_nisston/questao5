# Programa que lê uma lista de números e retorna a média dos números pares

lista_de_numeros = [3,15,17,51,53]

numeros_pares = [num for num in lista_de_numeros if num % 2 == 0]

if numeros_pares:
    media = sum(numeros_pares) / len(numeros_pares)
    print(f"A média dos números pares da lista é: {media}")

else:
    print("Não há números pares na lista.")